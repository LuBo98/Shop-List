CREATE TABLE IF NOT EXISTS items_to_get (
                                            id INT AUTO_INCREMENT PRIMARY KEY,
                                            name VARCHAR(255),
    amount VARCHAR(255)
    );